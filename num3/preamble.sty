\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english,russian]{babel}
\usepackage{graphicx}
\usepackage[sort,compress]{cite}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{fancyvrb}
\usepackage{longtable}
\usepackage{array}
\usepackage{minted}
\usepackage{tempora}
\usepackage[hidelinks]{hyperref}
\usepackage{tikz}
\usetikzlibrary{positioning, shapes.geometric, arrows.meta, matrix}

% Настройка minted
\setminted{style=vs, fontsize=\small, breaklines, framesep=2mm, baselinestretch=1.2}
\setminted[python]{linenos, autogobble}
\setminted[cpp]{linenos, autogobble}

% Глобальные настройки minted для C++
\setminted[cpp]{
    frame=lines,
    framesep=2mm,
    baselinestretch=1.2,
    fontsize=\small,
    linenos,
    style=bw
}

% Глобальные настройки minted для Python
\setminted[python]{
    frame=lines,
    framesep=2mm,
    baselinestretch=1.2,
    fontsize=\small,
    linenos,
    style=bw
}

% Настройка номеров строк minted
\renewcommand\theFancyVerbLine{\small\arabic{FancyVerbLine}}
